#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    unordered_set<int> hset;
    for (int i = 1; i <= n; i++) {
        hset.insert(i);
    }

    int lastRemoved = 0;
    while (hset.size()) {
        }
}