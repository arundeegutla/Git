import java.util.Scanner;

public class i {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        int nStrings = s.nextInt(), len = s.nextInt();
        String[] arr = new String[nStrings];

        for (int i = 0; i < nStrings; i++) {
            arr[i] = s.nextLine();
        }

        for (int i = 0; i < nStrings; i++) {
            String s1 = arr[i];
            String s2 = arr[i + 1];

            
        }
    }
}
